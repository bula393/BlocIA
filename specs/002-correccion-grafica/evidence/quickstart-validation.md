# Quickstart Validation Evidence

The scenarios in `quickstart.md` are covered by:

- `npm test`
- `npm run build`
- `npx playwright test`

Results are recorded in `frontend-tests.md`, `frontend-build.md`, and `playwright-tests.md`.
